<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top mb-5">
  <a class="navbar-brand text-white" href ="/">CORA</a>
        <% if user_signed_in? %>
        <span class="text-white">ようこそ <%= "#{current_user.name_last_name + current_user.name_first_name}"%>さん</span>
       <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
          <%# <li><%= link_to current_user.nickname, "#", class: "user-nickname" %>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <% if user_signed_in? %>
             <ul class="navbar-nav ml-auto">
              <%# <% if current_user.group_id != 2%>
              <li class="nav-item"><%= link_to "マイページ", user_path(current_user.id) ,class:"nav-link text-white"%></li>
            <%# <li class="nav-item text-white">|</li> %>
              <%# <% end %>
             <li class="nav-item"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "logout nav-link text-white" %></li>
            </ul>
          <% else %>
            <ul class="navbar-nav ml-auto">
             <li class="nav-item"><%= link_to  "新規登録", new_user_registration_path,class: "sign-up nav-link text-white" %></li>
               <%# <li class="nav-item text-white">|</li> %>
              <li class="nav-item"><%= link_to 'ログイン', new_user_session_path, class: "login nav-link text-white" %></li>
            </ul>
          <% end %>
        </div>
</nav>

<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>